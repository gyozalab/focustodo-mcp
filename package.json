{
  "name": "focustodo-mcp",
  "version": "2.0.0",
  "description": "MCP Server for Focus To-Do (專注清單)",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node --env-file-if-exists=.env dist/index.js",
    "dev": "tsx --env-file=.env src/index.ts",
    "selftest": "tsx --env-file=.env src/selftest.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20.12"
  }
}
