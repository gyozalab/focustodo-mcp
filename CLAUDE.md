# Focus To-Do MCP 工作區

本 repo 是 Focus To-Do 的 MCP server，工作區遷移後的路徑是 D:\workspaces\focustodo-mcp。
常用指令：npm run build、npm run dev、npm start。這個 server 使用逆向 API，變更前先讀 README.md 與 .env.example。
搬遷後的 MCP 設定需在 D:\coding 根目錄使用 claude mcp 指令更新；不要手改自動管理的設定檔。
服務正在使用時不要移動原路徑；發布或交付前讀 D:\coding\.agent\agents\mirror-of-truth\AGENT.md。
